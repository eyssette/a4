{
	// See https://go.microsoft.com/fwlink/?LinkId=733558
	// for the documentation about the tasks.json format
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Create A4 page",
			"type": "shell",
			"command": "pandoc --lua-filter=pandoc/fr-nbsp.lua ${file} -o ${fileBasenameNoExtension}.html -s --template=pandoc/template.html ",
			"group": {
				"kind": "build",
				"isDefault": true
			}
		}
	]
}